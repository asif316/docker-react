sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t asifhasan/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true asifhasan/docker-react npm run test
